

    function sub(a:number,b:number){
        return a - b;
    }
    function mul(c:number, d:number){
        return c * d;
    } 
   
    function div(a:number, b:number)
    {
        return a / b;
    }

   
   module.exports.sub = sub;
   module.exports.mul = mul;
   module.exports.div = div;